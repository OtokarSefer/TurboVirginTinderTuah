import Card from "../../components/UI/Card";
import React from "react";


// Idk if we even need this

const Chat = () => {
    return (
        <Card className="chat">
            <div>
                <h1>
                    Seeing the people youve matched with, and ur given the ability to chat
                </h1>
            </div>
        </Card>
    );
};

export default Chat;
